"""Application configuration for the django_totp package."""

from django.apps import AppConfig


class DjangoTotpConfig(AppConfig):
    """Django application configuration for django_totp."""

    name = "django_totp"
